Audit the HTTP handlers in this repository for input-validation, authorization, and error-handling bugs. The handler files are in src/handlers/ (there are exactly 4). Spawn ONE worker subagent per handler file (four workers total), launch them in parallel, and give each worker exactly one handler. Have each worker fully inspect its assigned handler plus the shared files src/models.js, src/utils.js, and src/middleware.js before reporting. Wait for all four workers, then merge their reports into one final audit report grouped by handler file.
